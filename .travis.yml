language: java
jdk:
- oraclejdk8
sudo: required

services:
- docker

script :
- echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
# mvn clean install dockerfile:push
- echo $TRAVIS_BRANCH
- if [ "$TRAVIS_BRANCH" == "feature/2-CI-Improvements" ]; then echo "If check is working"; else echo "else"; fi

install: true

cache:
  directories:
  - .autoconf
  - $HOME/.m2